<nav aria-label="breadcrumb">
    <ol class="breadcrumb {{ class|e('html_attr') }}" itemscope itemtype="https://schema.org/BreadcrumbList">
        {% for breadcrumb in breadcrumbs %}
            {% set breadcrumb_title = fn('html_entity_decode', breadcrumb.title|default(''), constant('ENT_QUOTES') b-or constant('ENT_HTML5'), 'UTF-8') %}
            <li class="breadcrumb-item {{ breadcrumb.class|e('html_attr') }}" itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem">
                {% if breadcrumb.url %}
                    <a href="{{ breadcrumb.url|e('html_attr') }}" itemprop="item">
                        <span itemprop="name">{{ breadcrumb_title|e }}</span>
                    </a>
                {% else %}
                    <span itemprop="name" aria-current="page">{{ breadcrumb_title|e }}</span>
                {% endif %}
                <meta itemprop="position" content="{{ loop.index|e('html_attr') }}">
            </li>
        {% endfor %}
    </ol>
</nav>
